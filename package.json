{
  "name": "domloo-release-action",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npx tsc && ncc build dist/index.js -o dist --minify && ncc build dist/push-engine.js -o dist --minify"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.1"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  }
}
